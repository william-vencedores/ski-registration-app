{
  "name": "vencedores-ski",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../server-lambda && npm install"
  },
  "devDependencies": {}
}
